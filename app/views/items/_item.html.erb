<div class="item">
  <p><%= item.content %>
  <%= link_to "", list_item_path(@list,item.id), method: :delete, class: 'glyphicon glyphicon-ok' %>
  <small>
    <i><%= item.days_left %> days left to finish!</i>
  </small>
</div>
